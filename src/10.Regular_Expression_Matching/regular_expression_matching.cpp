#include<string>
using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {
        
    }
};

int main()
{
    string s = "abd";
    string p = ".*";
    Solution A;
    bool res = A.isMatch(s, p);
    cout << res << endl;
    return 0;
}